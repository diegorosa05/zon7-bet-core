import type { ReactNode } from "react";

/** Mockup 100% liberado: nenhuma funcionalidade fica restrita. */
export function RestrictedGate({ children }: { children: ReactNode; recurso?: string }) {
  return <>{children}</>;
}
